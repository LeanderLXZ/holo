# Editor / IDE
.history/
.idea/
.vscode/
*.swp

# Python
__pycache__/
*.py[cod]
*.egg-info/
.mypy_cache/
.pytest_cache/
.ruff_cache/
.venv/
venv/

# Node
node_modules/

# OS
.DS_Store
Thumbs.db

# Local config
*.local.toml
*.local.json
.env
.env.local

# Build outputs / caches
dist/
build/
.cache/

# Smart-merge tmp root (project-configurable via
# `ai_context/skills_config.md ## Tmp directory`; default `./tmp/holo/`).
# Carries 4-agent translation template trees + Agent 1 merger staging
# output during `/holo:init` / `/holo:update`. Distinct from
# `logs/file_snapshots/` (persistent backups, NOT gitignored).
tmp/holo/

# Project-specific (extend as needed; mirror `ai_context/skills_config.md`
# §Do-not-commit paths so the two stay in sync)
